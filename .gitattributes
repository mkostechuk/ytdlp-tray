# Normalize line endings: use LF in the repo, let Git convert on checkout
* text=auto eol=lf

# Windows scripts should stay CRLF on Windows checkout
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf
